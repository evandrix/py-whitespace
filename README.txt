Whitespace -- Some utilities for whitespace normalization
---------------------------------------------------------


fixspace:                script to unmess whitespace in files
sane_whitespace_hook.py: mercurial hook to moan about bad space in commits

To install the whitespace hook into REPO just do
> ./install_sane_whitespace_hook.py REPO

Unittests can be run like so:

> py.test


